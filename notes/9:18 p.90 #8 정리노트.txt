Q. 한 라인에 ‘;’으로 5개의 이름을 구분하여 입력받아, 각 이름을 끊어내어 화면에 출력하고 가장 긴 이름 판별하기

1. getline은 세 번째 파라미터로 제한자(delim)을 입력할 수 있다.
cin.getline(name, 100, ‘;’); 과 같은 코드를 작성하게 되면 ;를 기준으로 이름들을 구분할 수 있게 된다.

2. 구분된 문자열들을 각 변수에 저장한 뒤에 한 줄씩 출력한다.

3. length 함수를 이용해 각 문자열들의 길이를 비교해 가장 큰 문자열을 출력한다.